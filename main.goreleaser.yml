builds:
  - main: ./cmd/tester/main.go
    binary: tester
    env:
      - CGO_ENABLED=0
    goarch: [amd64, arm64]
    goos: [linux, darwin]

archives:
  - name_template: "{{ .Tag }}_{{ .Os }}_{{ .Arch }}"
    format: tar.gz
    files:
      - test.sh
      - signature_printer
      - ls_darwin_amd64
      - ls_darwin_arm64
      - ls_linux_amd64
      - ls_linux_arm64